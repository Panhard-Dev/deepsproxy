/*
 * File: json.ts
 * Project: deepsproxy
 * Robust JSON parsing — re-exports the hardened implementation ported from
 * QwenProxy (lives in tools/json.ts alongside the StreamingToolParser).
 */

export { robustParseJSON } from '../tools/json.ts';
